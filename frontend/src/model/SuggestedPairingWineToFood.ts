//pairings is an object which can contain any amount of string[]. These properties can have arbitrary names and cannot be declared at compile time
export interface SuggestedPairingWineToFood {
    pairings: Object
}
